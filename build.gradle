plugins {
    id 'nva.upload.multipart.rootplugin'
}

//workaround for jacoco-merge to work
allprojects {
    apply plugin: 'nva.upload.multipart.java-conventions'
}

wrapper {
    distributionType = Wrapper.DistributionType.ALL
    gradleVersion = '8.2.1'
}